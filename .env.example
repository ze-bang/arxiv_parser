# arxiv_parser configuration
# Copy this file to .env and fill in the secrets as needed.

# arXiv category to monitor (default is the user request)
ARXIV_CATEGORY=cond-mat.str-el

# How many top papers to include in the daily digest
TOP_M=5

# SQLite DB path for subscribers and seen preprints
DB_PATH=data/app.db
# If using Vercel front-end subscriber list, set to HTTPS endpoint:
# e.g., https://your-app.vercel.app/api/subscribers
SUBSCRIBERS_URL=

# HTTP client settings
USER_AGENT=arxiv_parser/0.1 (+https://example.com/contact)
REQUEST_TIMEOUT=20

# ===== LLM (optional) =====
# OpenAI API key (leave blank to use abstract-only summaries)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# ===== SMTP (for email sending) =====
# SMTP server configuration (e.g., Gmail: smtp.gmail.com:587 with an App Password)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# Sender display name and email
EMAIL_FROM="CondMat Digest <you@example.com>"

# ===== Resend (optional alternative to SMTP) =====
# If set, Resend will be used instead of SMTP
RESEND_API_KEY=
RESEND_FROM="CondMat Digest <digest@yourdomain.com>"
